<-- if % if % else % sorted else =================================================================
//! If the condition is true, then execute code in the `if` block. Otherwise, if there's an `else` part and the condition is false,
// If the condition is true, executes the code block. Otherwise, skips it and moves to the next statement.
if (condition) {
    // Code Block 1 - executed when the 'condition' evaluates to true
} else {
    // Code Block 2 - executed when the 'condition' evaluates to false
}

// The "else" part is optional and executed when the 'condition' evaluates to false.
// If there is no "else" specified and the 'condition' is false, nothing will be executed.